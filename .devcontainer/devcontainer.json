{
  "name": "OpenStudio AI Harness (Python 3.10)",
  "dockerComposeFile": "../compose.yaml",
  "service": "devcontainer",
  "workspaceFolder": "/workspace/openstudio-ai-harness",
  "overrideCommand": false,
  "postCreateCommand": "uv sync --extra dev --locked && uv run --extra dev python -m playwright install chromium && uv sync --project standalone --locked",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  }
}
